# Copy to .env and fill in to enable the AI Illustrator page.
# WARNING: Vite inlines VITE_* variables into the public JS bundle. Anyone who loads the
# site can read this key. Restrict it by HTTP referrer and quota in Google AI Studio,
# or proxy the request through a server you control.
VITE_GEMINI_API_KEY=
